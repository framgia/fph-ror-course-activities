<h1>Edit User</h1>

<%= form_for(:user, url: "/users/#{@user.id}", method: :patch) do |f| %>
	<div>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>

	<div>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</div>

	<%= f.submit %>
<% end %>

<%= link_to "Back", "/" %>